# Publishes docs/ as a GitLab Pages site on the default branch.
image: alpine:latest

pages:
  stage: deploy
  script:
    - rm -rf public
    - mkdir public
    - cp -a docs/. public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
